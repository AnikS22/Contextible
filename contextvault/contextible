#!/bin/bash
# Contextible CLI Wrapper Script

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python contextible script
cd "$SCRIPT_DIR"
python3 contextible.py "$@"
